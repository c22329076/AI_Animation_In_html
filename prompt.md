# Prompt 模板集合

> 以下為 `original-prompt.txt` 的分類整理版，Prompt 原文原樣保留，僅最佳化排版結構。

---

## 1. 頁面生成

```
生成純前端頁面演示以上內容，使用偏暗的炫酷色調，多一些圖形效果，
只要有演示的效果即可，其它的網頁細節可以忽略，要求1000行以上程式碼
```

```
生成純前端頁面演示以上內容，使用偏暗的炫酷色調，以xx色為點綴，
多一些圖形效果，只要有演示的效果即可，其它的網頁細節可以忽略，
(要求1000行以上程式碼)，(同時使用類似PPT翻頁演示的效果)
```

```
生成基於純前端頁面進行演示xxx，使用白色為主色調加以其它炫酷顏色，
以粉紅色為點綴，多一些圖形效果，只要有演示的效果即可，
其它網頁細節可以忽略，要求1000行以上程式碼
```

```
生成基於純前端頁面進行演示以上內容，以background.html為背景，
多一些圖形效果，只要有演示的效果即可，其它的網頁細節可以忽略，
要求1000行以上程式碼
```

```
使用網頁前端程式碼生成一個粉紅色的資料卡，
內容是關於"雙鏈路接入"的科普
```

```
生成網頁模擬xx(防火牆基於IP地址與連接埠過濾流量)，
要求完成度高，800行以上程式碼
```

```
生成網頁 xx(防火牆基於IP地址與連接埠過濾流量)，使用偏暗的色調，
主要注重圖形與動態顯示效果，在一個螢幕內顯示，
要求完成度高，1000行以上程式碼
```

---

## 2. 動畫效果

```
為該網頁新增一個打開時的載入頁面，載入時間為3秒鐘
```

```
幫我實現在打開這個網頁時，各個元素依次"緩入"出現的動畫效果，
且在切換"場景一"與"場景二"時元素內容變化時也新增變化的動畫效果
```

```
實現在打開這個網頁時，除了標題和"class="section""的邊框與背景預設存在，
其它section內的各個元素依次"緩入"出現的動畫效果
```

```
修改各個動畫的播放時間，要求整個頁面載入完成用時五秒鐘
```

```
為網頁中的元素新增持續的"強調效果"動畫
```

```
為"xx"新增強調的效果(為"閘道器IP"欄位新增強調的效果)
```

```
幫我實現在打開這個網頁時，除了右上角的水印外其它各個元素依次"緩入"
出現的動畫效果，所有元素出現一共5s
```

```
實現讓div class="warning-text"中的文字內容也依次出現
```

```
我發現"ai-arch-diagram"模組內的動畫並沒有按"人類語言"、
（原始輸入箭頭）、"詞義表徵"、（詞向量箭頭）、"序列建模"（語義理解箭頭）、
和"AI 理解"的順序依次出現
```

```
對檔案進行動畫效果的精細化最佳化，要求實現頁面中所有功能模組及其包含的
小元素按預設順序依次出現的動畫效果。具體實施要求如下：
1. 為頁面中各大功能模組建立明確的層級動畫關係，確保父模組與子元素的動畫銜接自然
2. 按相同精細度標準，為頁面其他功能模組設計並實現類似的有序動畫序列
3. 確保所有動畫效果具有統一的時間曲線和過渡節奏，避免視覺混亂
4. 動畫持續時間、延遲時間和過渡效果應保持一致的設計語言
```

```
根據元素的內容型別最佳化動畫效果
```

```
為虛線箭頭元素新增持續迴圈的流動的動畫效果，
使箭頭在完成初始出現動畫後能夠保持動態狀態。
```

```
將箭頭替換為虛線箭頭。為虛線箭頭元素新增持續迴圈的流動的動畫效果，
使箭頭在完成初始出現動畫後能夠保持動態狀態。
```

```
為所有有滑鼠指標hover後發光效果的元素，
設定為預設就有hover的發光效果
```

```
在網頁中新增一些顏文字使其看起來很二次元
```

---

## 3. 3D 效果

```
剪映變形：左上 (-500X,500Y)  右上 (0X,0Y)
左下 (0X,0Y)                 右下 (-500X,500Y)
```

```
為該動畫新增3D效果
```

```
為X模組整體新增3D旋轉效果，實現滑鼠往頁面左側移動時該模組Y軸向左，
滑鼠往頁面右側移動時該模組Y軸向右
```

```
為X模組整體新增3D旋轉效果，實現該模組從Y軸向左15度移動到該模組Y軸向右15度，
每個迴圈60秒
```

---

## 4. PPT 風格

```
生成基於純前端頁面單頁佈局仿PPT換頁輪播進行直觀視覺化的xxx介紹
```

```
現要求將每個獨立功能模組(模組名稱)配置為獨佔一個完整頁面。
實現一個輪播圖（PPT）形式的頁面切換機制，使各個模組頁面能夠按照預設順序
自動輪流播放。系統應包含以下核心功能：
1) 模組頁面獨立渲染機制，確保每個模組擁有完整的頁面空間
2) 輪播控制功能，支援自動播放/暫停、手動上一頁/下一頁切換
3) 輪播參數配置，可自定義切換動畫效果、切換間隔時間、過渡時長
4) 響應式設計，確保在不同裝置上均能完整展示模組內容並保持輪播功能正常執行
```

```
根據以上內容生成基於純前端頁面單頁佈局仿PPT換頁輪播進行直觀圖形化視覺化的介紹，
加大文字大小，並運用加粗、下劃線、斜體、文字顏色、文字背景等強調方式
方便進行影片展示。新增每次切換頁面時頁面中的各個元素依次"緩入"
出現的動畫效果(細化到每行文字)。
將網頁的風格轉為暖色調風格(比如白色背景黑色文字加頁面粉色點綴)，
減少PPT演示的字數，增大文字大小，加強圖形化顯示，圖文並茂，方便影片展示。
將emoji圖示換成平面ui庫的圖示。將程式碼輸出到@/9-1.html 中
```

```
根據以上內容生成基於純前端頁面單頁佈局仿PPT換頁輪播進行直觀圖形化視覺化的介紹，
加大文字大小，並運用加粗、下劃線、斜體、文字顏色、文字背景等強調方式
方便進行影片展示。新增每次切換頁面時頁面中的各個元素依次"緩入"
出現的動畫效果(細化到每行文字)。
```

```
以頁面 Seedance 2.0.html 為模板重構 xxx
(模板：刪除第一頁的xxx和xxx，刪除xx頁面的暫停與重置按鈕，
設定為切換到這個頁面時等待1秒後自動迴圈播放)
```

```
以 PPT-Generate-3.html 為模板演示以上內容
```

```
將 x.html 網頁的第x和第x頁的內容合併後按照指定圖片的平面UI樣式進行視覺重構，
保持現有顏色方案不變。具體實施要求包括：
精確還原圖片中的佈局結構、元素間距、字型樣式、圖示設計和視覺層次；
確保所有互動元素（按鈕、表單、導航等）的視覺表現與參考圖片一致；
維持原有的顏色值和配色方案；保證修改後的頁面在不同裝置和瀏覽器上
具有良好的響應式表現；最佳化DOM結構以匹配UI設計，同時保持原有功能完整性和互動邏輯不變。
完成後需進行視覺一致性檢查，確保實現效果與參考圖片達到95%以上的相似度。
```

```
減少PPT演示的字數，增大文字大小，加強圖形化顯示，方便影片展示
```

```
加強PPT的圖形化演示，圖文並茂，方便影片展示
```

```
增加文字的大小，最佳化排版，運用加粗、下劃線、斜體、文字顏色、文字背景等強調方式，
使其能更好地在影片上演示
```

```
刪除頁面右上角的頁碼，頁面下方的頁碼，刪除頁面下方的"← → 鍵 / 滾輪 / 點擊切換"，
將頁面上方的進度條也刪除掉，但是保留鍵盤翻頁功能
```

```
基於檔案 x.html ，設計並開發一個視覺效果類似PPT首頁大版報風格的單頁網站，
用於展示"xxxxxx"主題內容。頁面應包含醒目的標題區域、核心概念簡介、
關鍵特性視覺化展示並確保所有內容都保持在一個頁面內。
設計需採用分層視覺效果，突出主題重點，使用適合學術主題的配色方案。
頁面元素應包括但不限於：大型標題文字、簡潔的主題相關內容的示意圖、
關鍵技術特點列表、以及適當的留白設計，以達到類似PPT首頁的視覺衝擊力和資訊傳達效果。
```

```
在檔案 x.html 中新增符合二次元風格的顏文字元素，以增強網頁的二次元視覺氛圍。
顏文字應與網頁內容主題相契合，放置在適當的位置如標題、段落分隔處或互動元素旁。
確保顏文字的新增不會影響網頁的正常佈局和功能，保持整體視覺協調與美觀。
建議選擇常見的二次元顏文字型別，並根據頁面設計風格調整顏文字的大小和顏色，
使其自然融入網頁整體設計。注意不要新增emoji表情符號，如果需要符號請新增ui庫的圖示
```

---

## 5. Trae（工具類）

```
重設這個網頁的佈局，美化這個網頁，使用tailwind樣式庫
```

```
將 1.html 的背景替換為 background.html 的背景
```

```
修改內容，將內容都壓縮到一個螢幕中
```

```
完善這個xxx的頁面內容與細節
```

```
將#script.js和#style.css都合併到#index.html中
```

```
將背景與頁面風格改為和 #Diffusion.html 一致
```

---

## 6. 美化

```
美化網頁內容與視覺化效果，新增動態效果，增強網頁動態性
```

```
根據現在的內容重構頁面，增強圖形化效果
```

```
為網頁中的元素新增"發光"或"泛光"的效果
```

```
為網頁中的元素(除標題外)新增滑鼠經過後"強調"的效果
```

```
為頁面中一些文字說明改成圖文並茂的描述，增強圖形化使其演示更形象
```

```
1.png 是一個少女的立繪，我希望把她放到 2.html 的第一個頁面中，
將立繪只露出上半部分，但是上半部分放大到能填充整個頁面的高度位置。
確保新增了人物之後不會影響原本頁面的翻頁功能
```

---

## 7. UI 置換（附圖參考）

```
將 x.html 網頁按照指定圖片的平面UI樣式進行視覺重構，
保持現有顏色方案不變。具體實施要求包括：
精確還原圖片中的佈局結構、元素間距、字型樣式、圖示設計和視覺層次；
確保所有互動元素（按鈕、表單、導航等）的視覺表現與參考圖片一致；
維持原有的顏色值和配色方案；保證修改後的頁面在不同裝置和瀏覽器上
具有良好的響應式表現；最佳化DOM結構以匹配UI設計，同時保持原有功能完整性和互動邏輯不變。
完成後需進行視覺一致性檢查，確保實現效果與參考圖片達到95%以上的相似度。
```

```
將 x.html 網頁的第六頁、第七頁、第八頁和第九頁的內容合併後
按照指定圖片的平面UI樣式進行視覺重構，保持現有顏色方案不變。
具體實施要求包括：
精確還原圖片中的佈局結構、元素間距、字型樣式、圖示設計和視覺層次；
確保所有互動元素（按鈕、表單、導航等）的視覺表現與參考圖片一致；
維持原有的顏色值和配色方案；保證修改後的頁面在不同裝置和瀏覽器上
具有良好的響應式表現；最佳化DOM結構以匹配UI設計，同時保持原有功能完整性和互動邏輯不變。
完成後需進行視覺一致性檢查，確保實現效果與參考圖片達到95%以上的相似度。
將重構的頁面儲存到 xx.html
```

```
將網頁修改為圖片中的平面ui設計樣式(配色不變)
```

```
將emoji圖示換成平面ui庫的圖示
```

